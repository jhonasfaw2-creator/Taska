/**
 * CSS variable generation utilities for the Taska Design System.
 *
 * Use `generateCssVariables()` to produce a complete :root block of
 * CSS custom properties from every design token.
 */
export { generateCssVariables, generateCssFile } from './generator';
